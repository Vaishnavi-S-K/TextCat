apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: https://textcat-prometheus.onrender.com
    isDefault: true
    editable: false
    jsonData:
      httpMethod: GET
      timeInterval: 5s
